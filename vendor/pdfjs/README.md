# PDF.js Vendor Files

This folder contains the browser build of `pdfjs-dist` version `5.6.205`.

Files:
- `pdf.min.mjs`
- `pdf.worker.min.mjs`
- `LICENSE`

The extension imports these files locally at runtime to parse GeM bid PDFs in memory. No remote script is loaded.
